# QueueCTL Environment Variables Example
# Copy this file to .env and update with your values if needed

# Database path (default: ./data/queuectl.db)
# QUEUECTL_DB_PATH=./data/queuectl.db

# Log level (default: INFO)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# QUEUECTL_LOG_LEVEL=INFO

# Log file path (default: ./data/queuectl.log)
# QUEUECTL_LOG_PATH=./data/queuectl.log
